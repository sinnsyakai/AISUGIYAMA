# AI Sugiyama - リカバリガイド

## 📋 概要

このドキュメントは、AI Sugiyamaの検索システムの問題診断と修正の記録です。

---

## 🔧 2024-12-14 検索システム修正

### 問題

1. **ベクトル検索が動作しない**: Google Embeddingsの類似度検索が日本語キーワードで正しく動作せず、「リコーダー」で検索しても全く関係ない結果が返される
2. **検索結果が毎回同じ**: クエリに関係なく同じドキュメントが返される
3. **502エラー**: LLM呼び出しとBM25検索の同時実行で負荷が高すぎてタイムアウト

### 解決策

| 問題 | 解決策 |
|------|--------|
| ベクトル検索の不具合 | **BM25キーワード検索に完全に置き換え** |
| 502エラー | **LLMクエリ生成を無効化**、シンプルな正規表現でキーワード抽出 |

---

## 🏗️ 現在のアーキテクチャ

```
ユーザー質問
    ↓
正規表現でキーワード抽出
    ↓
BM25検索（全ドキュメントをスキャン）
    ↓
上位10件を取得
    ↓
LLMで回答生成
```

### 主要コンポーネント

| ファイル | 役割 |
|----------|------|
| `app.py` | Streamlit UI + RAGパイプライン |
| `ingest.py` | データ取り込み・ベクトルDB構築 |
| `chroma_db/` | ChromaDBベクトルストア |
| `data/` | 元データ（PDF、TXT等） |

---

## 🛠️ トラブルシューティング

### 1. 検索結果がおかしい場合

```bash
# chroma_dbを再構築
rm -rf chroma_db
python3 ingest.py
```

### 2. 502エラー（Application failed to respond）

**原因**: メモリ不足またはタイムアウト

**対処**:
- LLMクエリ生成を無効化（現在は無効）
- BM25の検索件数を削減（k=10）
- 全ドキュメント取得の最適化

### 3. ローカルで検索テスト

```bash
python3 test_bm25.py
```

---

## 📦 依存関係

```
streamlit
langchain
langchain-chroma
langchain-google-genai
chromadb
rank-bm25
python-dotenv
pypdf
```

---

## 🚀 デプロイ

### Railway

1. GitHubにプッシュ → 自動デプロイ
2. 環境変数: `GOOGLE_API_KEY`

### ローカル

```bash
streamlit run app.py
```

---

## 📝 変更履歴

| 日時 | 変更内容 |
|------|----------|
| 2024-12-14 15:30 | BM25検索を実装、ベクトル検索を置き換え |
| 2024-12-14 16:00 | LLM意図推測を追加 |
| 2024-12-14 18:20 | 安定性のためLLMクエリ生成を無効化 |

---

## ⚠️ 既知の制限

1. **意図推測なし**: 「すぎやまって何者？」→「すぎやま」のみで検索（「経歴」「プロフィール」への拡張なし）
2. **メモリ使用量**: BM25は全ドキュメントをメモリに読み込むため、大量データには不向き

---

## 🔮 将来の改善案

1. **軽量な意図推測**: 辞書ベースでキーワード拡張（「何者」→「経歴 プロフィール」）
2. **BM25インデックスのキャッシュ**: 起動時に1回だけ構築
3. **ハイブリッド検索**: BM25 + ベクトル検索の組み合わせ
